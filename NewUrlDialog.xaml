<UserControl x:Class="MDM.NewUrlDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI"
             FocusManager.FocusedElement="{Binding ElementName=TxtUrl}">
    <Border x:Name="DlgChrome" Background="#1E1E1E" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="14" Margin="0" Width="540"
            ClipToBounds="True"
            UseLayoutRounding="True" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar — ana uygulama modalı ile aynı dil -->
            <Border x:Name="DlgTitleBar" Grid.Row="0" Background="#242424" CornerRadius="14,14,0,0" ClipToBounds="True">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,48,0">
                        <TextBlock x:Name="TxtTitleIcon" Text="&#xE71B;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                   Foreground="#FF6B00" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtTitle" Text="Yeni indirme" FontWeight="SemiBold" FontSize="13" Foreground="#F2F2F2"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="BtnCloseX" HorizontalAlignment="Right" VerticalAlignment="Stretch"
                            Content="&#xE8BB;" Width="44" FontFamily="Segoe MDL2 Assets" FontSize="10"
                            Click="BtnClose_Click" Cursor="Hand" Foreground="#FFFFFF" BorderThickness="0"
                            Background="Transparent" FocusVisualStyle="{x:Null}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="Transparent" CornerRadius="0,13,0,0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#E53935"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#C62828"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18,14,18,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TxtHint" Grid.Row="0"
                           Text="HTTP, FTP, SFTP, metalink, torrent veya magnet yapıştırın. Birden fazla adres için Ctrl+Enter. Sayfa taramak için ‘Sayfayı tara’."
                           Foreground="#9A9A9A" FontSize="12" Margin="0,0,0,12" TextWrapping="Wrap" LineHeight="18"/>

                <Border x:Name="UrlBox" Grid.Row="1" Background="#252525" BorderBrush="#333333" BorderThickness="1"
                        CornerRadius="10" MinHeight="100">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#333333"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#4A4A4A"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter Property="BorderBrush" Value="#FF6B00"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <TextBlock x:Name="TxtPlaceholder" Text="https://..." Foreground="#555" Margin="14,12,0,0"
                                   VerticalAlignment="Top" FontSize="13" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=TxtUrl}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBox x:Name="TxtUrl" Background="Transparent" BorderThickness="0"
                                 Foreground="#F0F0F0" CaretBrush="#FFFFFF" FontSize="13"
                                 AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 MinHeight="92" Padding="14,12"
                                 KeyDown="TxtUrl_KeyDown"/>
                    </Grid>
                </Border>

                <TextBlock x:Name="TxtValidation" Grid.Row="2" Text="" Foreground="#E07070" FontSize="11"
                           Margin="0,8,0,0" TextWrapping="Wrap" Visibility="Collapsed"/>

                <Grid Grid.Row="3" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="BtnTorrent" Grid.Column="0" Content="Torrent dosyası" MinWidth="120" Height="34"
                            HorizontalAlignment="Left" Margin="0" Padding="14,0"
                            Click="BtnBrowseTorrent_Click" Cursor="Hand" Foreground="#CCC" BorderThickness="0" FontSize="12">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="#2A2A2A" CornerRadius="9" Padding="{TemplateBinding Padding}"
                                        BorderBrush="#333333" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#333333"/>
                                        <Setter Property="Foreground" Value="#FF6B00"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#252525"/>
                                        <Setter Property="Foreground" Value="#E55A00"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <StackPanel x:Name="BtnRow" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="BtnCancel" Content="İptal" MinWidth="84" Height="34" Margin="0,0,8,0" Padding="14,0"
                                Click="BtnClose_Click" Cursor="Hand" Foreground="#CCC" BorderThickness="0" FontSize="12">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bd" Background="#2A2A2A" CornerRadius="9" Padding="{TemplateBinding Padding}"
                                            BorderBrush="#333333" BorderThickness="1">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#333333"/>
                                            <Setter Property="Foreground" Value="#F0F0F0"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#252525"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="BtnGrab" Content="Sayfayı tara" MinWidth="104" Height="34" Margin="0,0,8,0" Padding="14,0"
                                Click="BtnGrab_Click" Cursor="Hand" Foreground="#CCC" BorderThickness="0" FontSize="12">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bd" Background="#2A2A2A" CornerRadius="9" Padding="{TemplateBinding Padding}"
                                            BorderBrush="#333333" BorderThickness="1">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#333333"/>
                                            <Setter Property="Foreground" Value="#F0F0F0"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#252525"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="BtnDownload" Content="İndir" MinWidth="96" Height="34" Padding="16,0" Click="BtnDownload_Click"
                                Cursor="Hand" Foreground="White" BorderThickness="0" FontSize="12" FontWeight="SemiBold"
                                IsDefault="True">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bd" Background="#FF6B00" CornerRadius="9" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#FF8533"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#E55A00"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
