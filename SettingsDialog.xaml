<UserControl x:Class="DownloadMuck.SettingsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style x:Key="SettingsNavButton" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="Transparent" CornerRadius="8" Padding="12,9">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#222018"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2A2118"/>
                                <Setter Property="Foreground" Value="#FF6B00"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SoftButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#DDD"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="#252525" CornerRadius="8" Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#1B1B1B" BorderBrush="#333" BorderThickness="1" CornerRadius="14"
            Width="620" Height="420" MaxWidth="680" MaxHeight="520">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.55" Color="#000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Background="#1F1F1F" CornerRadius="14,14,0,0">
                <Grid>
                    <TextBlock Text="Ayarlar" Margin="16,0,0,0" VerticalAlignment="Center"
                               FontWeight="SemiBold" FontSize="13" Foreground="#E0E0E0"/>
                    <Button HorizontalAlignment="Right" Width="40" Height="38" Content="✕" Click="BtnCancel_Click"
                            Foreground="#AAA" BorderThickness="0" Background="Transparent" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="Transparent" CornerRadius="0,13,0,0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#E53935"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="168"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="#161616" BorderBrush="#262626" BorderThickness="0,0,1,0">
                    <StackPanel Margin="10,12,10,12">
                        <TextBlock Text="BÖLÜMLER" Foreground="#666" FontSize="10" FontWeight="SemiBold"
                                   Margin="8,0,0,10"/>
                        <RadioButton x:Name="NavGeneral" Content="Genel" GroupName="SettingsNav"
                                     Style="{StaticResource SettingsNavButton}" IsChecked="True"
                                     Checked="Nav_Checked"/>
                        <RadioButton x:Name="NavExtension" Content="Tarayıcı eklentisi" GroupName="SettingsNav"
                                     Style="{StaticResource SettingsNavButton}"
                                     Checked="Nav_Checked"/>
                        <RadioButton x:Name="NavUpdate" Content="Güncelleme" GroupName="SettingsNav"
                                     Style="{StaticResource SettingsNavButton}"
                                     Checked="Nav_Checked"/>
                        <RadioButton x:Name="NavAbout" Content="Hakkında" GroupName="SettingsNav"
                                     Style="{StaticResource SettingsNavButton}"
                                     Checked="Nav_Checked"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Column="1" Margin="16,14,16,8">
                    <ScrollViewer x:Name="PanelGeneral" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Varsayılan indirme klasörü" Foreground="#BBB" FontSize="11" Margin="0,0,0,8"/>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtFolder" Height="32" FontSize="11" VerticalContentAlignment="Center"
                                         Background="#252525" Foreground="#E0E0E0" BorderBrush="#333" BorderThickness="1"
                                         Padding="10,0"/>
                                <Button Grid.Column="1" Content="Gözat" Width="68" Height="32" Margin="8,0,0,0"
                                        Click="BtnBrowse_Click" Style="{StaticResource SoftButton}" Foreground="#E0E0E0"/>
                            </Grid>

                            <Border Background="#141414" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="10" Padding="12">
                                <StackPanel>
                                    <CheckBox x:Name="ChkAutoStart" Content="Windows ile birlikte başlat"
                                              Foreground="#E0E0E0" FontSize="12" Margin="0,0,0,10" Cursor="Hand"
                                              Checked="ChkAutoStart_Changed" Unchecked="ChkAutoStart_Changed"/>
                                    <CheckBox x:Name="ChkAutoStartMin" Content="Başlangıçta arka planda aç"
                                              Foreground="#E0E0E0" FontSize="12" Cursor="Hand"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer x:Name="PanelExtension" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Eklenti klasörü" Foreground="#BBB" FontSize="11" Margin="0,0,0,8"/>
                            <Border Background="#141414" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,0,0,14">
                                <TextBlock x:Name="TxtExtPath" Foreground="#999" FontSize="11" TextWrapping="Wrap"/>
                            </Border>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Kalıntı temizle" Margin="0,0,8,0"
                                        Click="BtnReinstallExt_Click" Style="{StaticResource SoftButton}" Foreground="#E0E0E0"/>
                                <Button Content="Klasörü aç"
                                        Click="BtnOpenExtFolder_Click" Style="{StaticResource SoftButton}"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer x:Name="PanelUpdate" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Uygulama güncellemesi" Foreground="#BBB" FontSize="11" Margin="0,0,0,8"/>
                            <Border Background="#141414" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock x:Name="TxtUpdateCurrent" Foreground="#E0E0E0" FontSize="12" Margin="0,0,0,8"/>
                                    <TextBlock Text="Güncelleme yalnızca buradan denetlenir; uygulama her açılışta otomatik aramaz."
                                               Foreground="#777" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"/>
                                    <TextBlock x:Name="TxtUpdateStatus" Foreground="#999" FontSize="11" TextWrapping="Wrap"
                                               Margin="0,0,0,12" MinHeight="18"/>
                                    <Button x:Name="BtnCheckUpdate" Content="Güncellemeyi denetle" HorizontalAlignment="Left"
                                            MinWidth="160" Height="32" Click="BtnCheckUpdate_Click"
                                            Cursor="Hand" Foreground="White" BorderThickness="0" FontSize="12" FontWeight="SemiBold">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="bd" Background="#FF6B00" CornerRadius="8" Padding="14,0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="#FF8533"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="bd" Property="Background" Value="#3A3A3A"/>
                                                        <Setter Property="Foreground" Value="#888"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer x:Name="PanelAbout" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                        <StackPanel>
                            <Border Background="#141414" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="10" Padding="16">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <Image Source="pack://application:,,,/Assets/mdm-logo.png" Width="36" Height="36"
                                               Margin="0,0,12,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="MuckDownloadManager" Foreground="#E0E0E0" FontSize="15" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="TxtVersion" Foreground="#999" FontSize="11" Margin="0,3,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <TextBlock Text="İndirmeleri kategorilere ayıran, tarayıcı yakalamalı masaüstü yöneticisi."
                                               Foreground="#777" FontSize="11" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <Border Grid.Row="2" Background="#1A1A1A" BorderBrush="#262626" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,14,14" Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="İptal" Width="84" Height="32" Margin="0,0,8,0" Click="BtnCancel_Click"
                            Style="{StaticResource SoftButton}"/>
                    <Button Content="Kaydet" Width="84" Height="32" Click="BtnSave_Click"
                            Cursor="Hand" Foreground="White" BorderThickness="0" FontSize="12" FontWeight="SemiBold">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="#FF6B00" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#FF8533"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
